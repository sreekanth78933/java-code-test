hello
devops new btach
devops practice session

pipeline {
  agent any {
  tools:
  stages {
     stage ("build" {
       sh 'mvn build' {
     }

amazon project -> release code 1.0 version to the end user

